/**
    const - постоянная.
    Хранит не изменяемое значение.
    При попытке изменить значение const ошибка.
    Если const ссылочный тип данных (массив, объект), то ссылка сохраненная в const неизменна, а аттрибуты ссылки можно изменять
*/

const PI = Math.PI;

console.log(PI)